def solution(A, K):
    if len(A) <=1 or len(A) ==K:
        return A
    else:
        if K >len(A):
            K = K%len(A)
        elif K <0:
            K = len(A)-K
        else:
            K = K
        pre = A[-K:]
        post = A[0:len(A)-K]
        return (pre + post)
